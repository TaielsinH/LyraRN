import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mapContainer: {
    width: "100%",
    height: 180,
    borderRadius: 8,
    overflow: "hidden",
    marginBottom: 12,
    borderWidth: 1,
    borderColor: "#334155",
  },
  map: {
    width: "100%",
    height: "100%",
  },
});
