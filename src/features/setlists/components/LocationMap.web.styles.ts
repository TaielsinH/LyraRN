import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mapFallback: {
    borderWidth: 1,
    borderColor: "#334155",
    borderRadius: 8,
    padding: 16,
    marginBottom: 12,
    backgroundColor: "#0f172a",
  },
  mapFallbackTitle: {
    color: "#ffffff",
    fontSize: 15,
    fontWeight: "700",
    marginBottom: 8,
  },
  mapFallbackText: {
    color: "#9ca3af",
    fontSize: 14,
  },
});
